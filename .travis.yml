language: python

sudo: false

python:
  - "2.7"

notifications:
  email: false

git:
  depth: 10

compiler:
  - clang
  - gcc

# Execute tests
script:
  - make build
  - make test
  - git status tests
  - git diff --quiet tests
